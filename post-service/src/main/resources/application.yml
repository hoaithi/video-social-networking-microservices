server:
  port: 8083
spring:
  application:
  name: post-service
  data:
    mongodb:
      uri: mongodb://root:root@localhost:27017/post-service?authSource=admin
springdoc:
  group-configs:
    - group: post
      paths-to-match: /post/**
